services:
  postgres:
    image: postgres:18
    container_name: account_reservation_postgres
    environment:
      POSTGRES_DB: account_reservation
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: root
    volumes:
      - postgres_data:/var/lib/postgresql/
    ports:
      - "5432:5432"


volumes:
  postgres_data: